export const CANVAS_SIZE = window.innerWidth <= 768 ? 300 : 500; // Responsive canvas size
export const TEXTURE_SIZE = 2048; // Texture size for WebGL rendering
export const MIN_TEXTBOX_FONT_SIZE = 12;
